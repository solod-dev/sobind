// An umbrella header holds no declarations of its own, only includes of the
// library's headers. Without -scope it emits nothing; with -scope pointed at
// the umbrella/ tree the included declarations come through.
#include "umbrella/part.h"
